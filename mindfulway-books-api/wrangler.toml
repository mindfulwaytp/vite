name = "mindfulway-books-api"
main = "src/index.js"
compatibility_date = "2024-07-02"
account_id = "c00ac805d54a0aefdf72b40e5f24bc82"

routes = [
  { pattern = "api.mindfulway-therapy.com/*", zone_name = "mindfulway-therapy.com" }
]

# Superseded by netlify/functions/airtableBooks.js — this worker is no longer deployed.
# Secrets must never be committed here; use `wrangler secret put AIRTABLE_TOKEN` instead.
[vars]
AIRTABLE_BASE_ID = "appjQb7xeAtRjHH19"
